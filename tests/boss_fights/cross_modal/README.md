# Cross-Modality Boss Fights

These fixtures deliberately make DOM, AX, and network evidence disagree. The gate asks whether the combined index preserves and exposes the contradiction, not whether it chooses an authoritative modality.
