- 1 extract selectors to "reverse retrieve" the content
- 2 filter based on sites that are permissively scraped and not
- 3 attach the relevant data in DB for later use. Every source should have its own

What this will take,  overrideing scrapegraph for specific use cases, not all have to be AI runs. This makes things simpler
